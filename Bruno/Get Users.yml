info:
  name: Get Users
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5001/users?ids=1
  headers:
    - name: "1"
      value: ""
      disabled: true
  params:
    - name: ids
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
